import AddressCard from './address-card.svg';
export {AddressCard};

import {View, Text} from 'react-native';
import React from 'react';

const index = ({name, size, style}) => {
  return <View />;
};

index.defaul;

export default index;
